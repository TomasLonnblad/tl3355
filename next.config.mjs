/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',

      },
      {
        protocol: 'https',
     
      },
    ],
  },
}

module.exports = nextConfig
